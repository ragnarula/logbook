# Copy to .env and adjust. .env is never committed.

# Where the SQLite file lives on the host, for the compose deployment.
APPDATA=/var/lib

# Leave empty to run with no authentication, which is only safe on a private
# network. Set a passcode to require sign-in — do this before exposing the
# service to the internet.
TRACKER_PASSCODE=
